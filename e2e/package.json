{
    "name": "better-blog-e2e",
    "version": "0.0.0",
    "private": true,
    "description": "End-to-end tests for Better Blog (Playwright)",
    "scripts": {
        "e2e:install": "playwright install --with-deps",
        "e2e": "playwright test",
        "e2e:ui": "playwright test --ui"
    },
    "devDependencies": {
        "@playwright/test": "^1.48.2",
        "tsx": "^4.20.3"
    }
}

