# React SPA configuration
# Provider can be 'memory' for client-side only or 'api' for API backend
VITE_BLOG_PROVIDER=memory

# Only needed when VITE_BLOG_PROVIDER=api
VITE_API_BASE_PATH=/api/posts
VITE_API_BASE_URL=http://localhost:3005

# Node.js API server config (only used with server.mjs)
PORT=3005
HOST=127.0.0.1
API_BASE_PATH=/api/posts
BETTER_BLOG_PROVIDER=memory
