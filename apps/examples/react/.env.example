HOST=127.0.0.1
PORT=3004

# Better Blog demo config
# Use 'memory' for in-memory data, 'api' to call the app's API routes
BETTER_BLOG_PROVIDER=memory

# Only used when BETTER_BLOG_PROVIDER=api
API_BASE_PATH=/api/posts

# Vite build-time mirrors (optional; the e2e runner maps these automatically if missing)
VITE_BLOG_PROVIDER=
VITE_API_BASE_PATH=